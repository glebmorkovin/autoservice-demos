import {data,view,mapUrl,tel} from '../data';
import {Arrow,Call} from '../components/Shell';

export default function Contacts(){return <section className="section contacts" id="contacts"><div className="contact-copy"><p className="kicker">Как нас найти</p><h2>{data.business.address}</h2><p className="contact-location">{data.business.location}</p><p className="contact-hours">{data.business.hours}</p>{view.mapNote&&<p className="map-note">{view.mapNote}</p>}<div className="contact-phones">{data.business.phones.map(number=><a href={tel(number)} key={number}>{number}</a>)}</div><div className="contact-actions"><Call/><a className="button button-outline" href={data.maps.yandex} target="_blank" rel="noreferrer">Построить маршрут<Arrow/></a></div></div><div className="map-panel"><iframe title={'Интерактивная карта: '+data.business.name} src={mapUrl()} loading="eager"/><div className="map-links"><a href={data.maps.yandex} target="_blank" rel="noreferrer">Яндекс Карты<Arrow/></a><a href={data.maps['2gis']} target="_blank" rel="noreferrer">2ГИС<Arrow/></a></div></div></section>}
