import {d,Photo,phone,Arrow,Call} from '../components/UI';
function title(s:string){return (s[0].toUpperCase()+s.slice(1)).replaceAll('/',' / ')}
export function Services(){const items=d.services.filter(s=>s!=='автосервис');const descriptions:Record<string,string>=d.commercial.serviceDescriptions;return <section id="services" className={'section services services-'+d.serviceStyle}><div className="section-heading"><p className="eyebrow">Направления работы</p><h2>{d.heading}</h2><p>{d.commercial.serviceLead}</p></div><div className="service-list">{items.map((s,i)=><article className="service-item" key={s}><span className="number">{String(i+1).padStart(2,'0')}</span><h3>{title(s)}</h3>{i<6&&<p>{descriptions[s]}</p>}<a href={phone} aria-label={'Записаться: '+s}><span>Записаться</span><Arrow/></a></article>)}</div></section>}
export function Features(){return <section className="features" id={d.commercial.sections.includes('services')?'details':'services'}><div className="feature-intro"><p className="eyebrow">{d.business.name}</p><h2>{d.commercial.featureHeading}</h2><p>{!d.commercial.sections.includes('services')&&d.commercial.serviceLead}</p></div>{[1,2,3].map(n=><article key={n} className={'feature feature-'+n}><Photo index={n}/><div><small>{String(n).padStart(2,'0')} / {d.business.name}</small><h3>{d.feature[n-1]}</h3><p>{d.commercial.featureText[n-1]}</p><a className="text-link" href={phone}>{d.commercial.cta}<Arrow/></a></div></article>)}</section>}
export function Price(){return <section className="price section" id="price"><div><p className="eyebrow">Запись и стоимость</p><h2>{d.commercial.priceTitle}</h2><p>{d.commercial.priceText}</p><Call label="Узнать стоимость"/></div><aside className="price-guide"><small>Для расчёта по телефону</small><ol>{d.commercial.priceChecklist.map((t,i)=><li key={t}><span>0{i+1}</span>{t}</li>)}</ol><a href={phone}>{d.business.phones[0]}<Arrow/></a></aside></section>}
export function Process(){return null}
